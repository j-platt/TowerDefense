#include "pathing_field.h"

tower_defense::pathing_field tower_defense::calculate_pathing(board const& source)
{
	pathing_field reply;
	//for(tile sourceRow[board_y_dimension] : reply.vectorField)
	//{

	//}
	//for(std::array<tile, board_y_dimension> const& sourceRow : source.tiles)
	//{
	//	for(tile const& sourceTile : sourceRow)
	//	{
	//		
	//	}
	//}
	return reply;
}